export = keypress;
/**
 * This module offers the internal "keypress" functionality from node-core's
 * `readline` module, for your own programs and modules to use.
 *
 * The `keypress` function accepts a readable Stream instance and makes it
 * emit "keypress" events.
 *
 * Usage:
 *
 * ``` js
 * require('keypress')(process.stdin);
 *
 * process.stdin.on('keypress', function (ch, key) {
 *   console.log(ch, key);
 *   if (key.ctrl && key.name == 'c') {
 *     process.stdin.pause();
 *   }
 * });
 * proces.stdin.resume();
 * ```
 *
 * @param {Stream} stream
 * @api public
 */
declare function keypress(stream: NodeJS.ReadStream): void;
declare namespace keypress {
    export { enableMouse, disableMouse };
}
declare function enableMouse(stream: NodeJS.ReadStream): void;
declare function disableMouse(stream: NodeJS.ReadStream): void;
